# file not in gitignore carful no to put any keys here dbt will check in .env
train_pipeline:
  outputs:
    dev:
      type: duckdb
      path: "md:train_pipeline?motherduck_token={{ env_var('MOTHERDUCK_TOKEN') }}"
      threads: 4

  target: dev
